# euphemera publisher — self-contained; runs on the host while the backend is in Docker.
# Versions verified on Python 3.14.
httpx==0.28.1
pytumblr==0.1.2
playwright==1.60.0
python-dotenv==1.2.2
requests-oauthlib==2.0.0  # only for get_tokens.py (console fallback)
# after install:  python -m playwright install chromium
